{
  "name": "forge-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.309",
    "esbuild": "^0.25.0"
  }
}
